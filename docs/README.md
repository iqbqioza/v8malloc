# User-facing documentation

This directory holds documentation that ships with the library (man
pages under `../man/`, API-reference HTML, tutorials).

The **design** documentation — architecture, algorithms, size-class
tables, benchmark protocol, architecture abstraction layer — lives
under [`../.claude/docs/`](../.claude/docs/) and is the authoritative
spec for the implementation.
